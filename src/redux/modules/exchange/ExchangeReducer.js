const initialState = {

}

const ExchangeReducer = (state = initialState, action) => {
    switch (action.type) {

        default: {
            return state;
        }
    }
};

export default ExchangeReducer;